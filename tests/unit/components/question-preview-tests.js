import { test , moduleForComponent } from 'appkit/tests/helpers/module_for';
import QuestionPreviewComponent from 'appkit/components/question-preview';

var component;

moduleForComponent('question-preview', 'Unit - Question preview component', 
                   {
  setup: function() {
    component = this.subject();
  }
});

test('Write a test here', function() {
  ok( false, 'Implement Me' );
});
